import React, { useEffect, useState, useCallback, useMemo } from "react";

import { Col, Container } from "reactstrap";

const CrmContacts = () => {
  return (
    <React.Fragment>
      <div className="page-content">
        <Container fluid>
          <h1> i am for all restaurent kds</h1>
        </Container>
      </div>
    </React.Fragment>
  );
};

export default CrmContacts;
